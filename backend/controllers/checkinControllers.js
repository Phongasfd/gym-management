const prisma = require('../prisma');

// Get all checkins
const getAllCheckins = async (req, res) => {};

// Create new checkins
const createCheckin = async (req, res) => {};

module.exports = {
  getAllCheckins,
  createCheckin
};
